import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF00462D);
  static const primaryContainer = Color(0xFF1F5E43);

  static const secondary = Color(0xFFFECE4B);

  static const background = Color(0xFFFEF7FF);

  static const surface = Color(0xFFFFFFFF);

  static const text = Color(0xFF1D1A22);

  static const outline = Color(0xFFBFC9C1);

  static const grey = Color(0xFF707973);

  // Matches the cream background baked into the AflAlert logo artwork.
  static const logoCream = Color(0xFFF6F4E2);

  // T95 tone — replaces the plain white theme surface on auth screens.
  static const t95 = Color(0xFFFFEFD0);
}