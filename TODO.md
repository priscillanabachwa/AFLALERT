# TODO

## Goal: View the login screen on Pixel 8 emulator

1. Confirm app entry route points to `LoginScreen`.
2. Build app for Android emulator.
3. Fix Android build failure: JVM target mismatch (Java 11 vs Kotlin 21) in `tflite_flutter`.
4. Re-run `flutter run -d emulator-5554` and verify login UI appears.


